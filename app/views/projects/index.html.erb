<div class = "container">
  <div class = "page-header">
    <h1>Projects</h1>
    <%= link_to "Create Project", new_project_path, class: 'btn btn-primary pull-right' %>
  </div>

<h3>Name</h3>
    <% @projects.each do |project| %>
    <p><%= link_to project.name, project_path(project) %></p><hr>
    <% end %>


</div>
