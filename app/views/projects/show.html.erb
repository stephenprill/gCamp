
<div class = "container">


  <p id="notice"><%= notice %></p>


<table class="table">
  <thead>
  </thead>
<tbody>
  <tr>
<td class="col-md-8"><h1><%= @project.name %></h1></td>
<td class="col-md-2"><%= link_to 'Edit', edit_project_path(@project), class: 'btn btn-info pull-right'  %></td>
<td class="col-md-2"><%= link_to 'Delete', project_path(@project), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger pull-right' %></td>
</tr>
</tbody>
</table>
</div>
